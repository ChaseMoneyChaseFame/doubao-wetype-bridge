# 贡献指南

1. Fork 仓库并从 `main` 创建分支。
2. 保持修改范围聚焦，为状态机变更补充测试。
3. 运行 `swift run BridgeSelfTests`、`swift test` 和 `Scripts/build-app.sh`。
4. 提交 Pull Request，说明 macOS、豆包输入法和微信输入法版本。

请勿提交用户日志、开发者证书、公证密钥或其他凭据。
